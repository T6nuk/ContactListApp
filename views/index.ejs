<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact list</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

    <div class="box" id="heading">
        <h1><%= date %> </h1>
    </div>

    <div class="box">
        <% for(let i = 0; i < myContacts.length; i++) { %>
            <form action="/delete" method="POST">
                <div class="item">
                    <input type="checkbox" value="<%= myContacts[i].phone%>" name="checkbox" onchange="this.form.submit()" >
                    <p><%= myContacts[i].name %></p>
                    <p><%= myContacts[i].phone %></p>
                </div>
            </form>
            
        <% } %>    

        

        <form action="/" class="class" method="POST">
            <input type="text" name="newName" placeholder="Contact name">
            <input type="text" name="newPhoneNumber" placeholder="Contact phone number">
            <br>
            <button type="submit">+</button>
        </form>
    </div>
    
    <footer>
        <span>Tõnu K. &#169; 2021</span>
    </footer>
</body>
</html>