<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact list</title>
    <script src="https://kit.fontawesome.com/fdfd6c296d.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

    <div class="box" id="heading">
        <h1><%= date %> </h1>
    </div>

    <div class="box">
        <% for(let i = 0; i < myContacts.length; i++) { %>
            <form action="/delete" method="POST">
                <div class="item">
                    <p><%= myContacts[i].name %></p>
                    <p><%= myContacts[i].phone %></p>
                    <button type="submit" style="margin-inline-start: auto;" value="<%= myContacts[i].phone%>"  name="checkbox" ><i class="fas fa-minus"></i></button>
                    
                </div>
            </form>
            
        <% } %>    

        

        <form action="/" class="class" method="POST">
            <input type="text" name="newName" placeholder="Contact name">
            <input type="text" name="newPhoneNumber" placeholder="Contact phone number">
            <br>
            <button type="submit"><i class="fas fa-plus"></i></button>
        </form>
    </div>
    
    <footer>
        <span>Tõnu K. &#169; 2021</span>
    </footer>
</body>
</html>